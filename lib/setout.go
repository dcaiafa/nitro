package lib

import (
	"context"

	"github.com/dcaiafa/nitro"
)

func fnSetOut(ctx context.Context, caps []nitro.ValueRef, args []nitro.Value, retN int) ([]nitro.Value, error) {
	//prevOut := Stdout(ctx)
	return nil, nil
}
